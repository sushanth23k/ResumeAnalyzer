# Resume Generator API Integration - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Frontend Application                         │
│                      (React + TypeScript + Vite)                     │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTP Requests
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        API Service Layer                             │
│                      (src/services/api.ts)                           │
├─────────────────────────────────────────────────────────────────────┤
│  • experienceGeneratorApi.generate()                                │
│  • projectGeneratorApi.generate()                                   │
│  • skillsGeneratorApi.generate()                                    │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ POST Requests
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        Backend API Server                            │
│                  (Django REST @ http://127.0.0.1:8000)              │
├─────────────────────────────────────────────────────────────────────┤
│  POST /analyzer/experience-gen                                      │
│  POST /analyzer/project-gen                                         │
│  POST /analyzer/skill-gen                                           │
└─────────────────────────────────────────────────────────────────────┘
```

## Component Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                           ResumeContext                              │
│                    (Global State Management)                         │
├─────────────────────────────────────────────────────────────────────┤
│  State:                                                              │
│  • generatorOutput: { experiences, projects, skills }               │
│  • rawExperienceData: GeneratedExperienceItem[]                     │
│  • rawProjectData: GeneratedProjectItem[]                           │
└─────────────────────────────────────────────────────────────────────┘
                    │                │                │
                    │                │                │
        ┌───────────┘                │                └───────────┐
        ▼                            ▼                            ▼
┌───────────────┐         ┌───────────────┐         ┌───────────────┐
│  Experience   │         │   Project     │         │    Skills     │
│   Generator   │────────▶│   Generator   │────────▶│   Generator   │
│               │  Step 1 │               │  Step 2 │               │
└───────────────┘         └───────────────┘         └───────────────┘
        │                            │                            │
        │ Stores                     │ Stores                     │ Uses both
        │ rawExperienceData          │ rawProjectData             │ raw data
        ▼                            ▼                            ▼
        Context                      Context                      Context
```

## Data Flow - Experience Generator

```
┌──────────────────────────────────────────────────────────────────┐
│                     Experience Generator UI                       │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ User Input
                              │ • Job Role
                              │ • Job Description
                              │ • Bullet Counts [5,4,3]
                              │ • Side Prompt
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    experienceGeneratorApi.generate()             │
│                       (src/services/api.ts)                      │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ POST Request
                              │ {
                              │   job_role,
                              │   job_description,
                              │   points_count: [5,4,3],
                              │   additional_instruction
                              │ }
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│              Backend: /analyzer/experience-gen                    │
│                    (AI Processing)                                │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ JSON Response
                              │ {
                              │   status: "success",
                              │   output: [
                              │     {
                              │       experience_id: 1,
                              │       experience_company_name: "...",
                              │       experience_role: "...",
                              │       resume_points: [...],
                              │       start_date: "...",
                              │       end_date: "..."
                              │     }
                              │   ]
                              │ }
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                      Frontend Processing                          │
│  1. Store raw response → setRawExperienceData(response.output)   │
│  2. Transform to frontend format                                  │
│  3. Update UI → setGeneratedExperiences(transformed)             │
│  4. Update context → setGeneratorOutput(...)                     │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                    Display Generated Content                      │
│                    Allow User to Edit                             │
│                    Proceed to Next Step                           │
└──────────────────────────────────────────────────────────────────┘
```

## Data Flow - Project Generator

```
┌──────────────────────────────────────────────────────────────────┐
│                      Project Generator UI                         │
│                (Job Role/Description Pre-filled)                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ User Input
                              │ • Line Counts [3,2]
                              │ • Side Prompt
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                     projectGeneratorApi.generate()               │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ POST Request
                              │ {
                              │   job_role,
                              │   job_description,
                              │   points_count: [3,2],
                              │   additional_instruction
                              │ }
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│               Backend: /analyzer/project-gen                      │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ JSON Response
                              │ {
                              │   status: "success",
                              │   output: [
                              │     {
                              │       project_id: 1,
                              │       project_name: "...",
                              │       project_points: [...],
                              │       project_skills: [...]
                              │     }
                              │   ]
                              │ }
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                      Frontend Processing                          │
│  1. Store raw response → setRawProjectData(response.output)      │
│  2. Transform: join project_points with spaces                    │
│  3. Update UI → setGeneratedProjects(transformed)                │
│  4. Update context → setGeneratorOutput(...)                     │
└──────────────────────────────────────────────────────────────────┘
```

## Data Flow - Skills Generator

```
┌──────────────────────────────────────────────────────────────────┐
│                       Skills Generator UI                         │
│                (Job Role/Description Pre-filled)                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ User Input
                              │ • Include Web Research (checkbox)
                              │ • Side Prompt
                              │
                              │ From Context:
                              │ • rawExperienceData
                              │ • rawProjectData
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                     skillsGeneratorApi.generate()                │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ POST Request
                              │ {
                              │   job_role,
                              │   job_description,
                              │   additional_instruction,
                              │   include_web_research: true,
                              │   experience_data: [...],  ← Raw API data
                              │   project_data: [...]      ← Raw API data
                              │ }
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                Backend: /analyzer/skill-gen                       │
│          (Uses experience_data + project_data for context)        │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ JSON Response
                              │ {
                              │   status: "success",
                              │   output: [
                              │     {
                              │       skill_category: "Programming",
                              │       skills: ["Java", "Python"]
                              │     },
                              │     {
                              │       skill_category: "Cloud",
                              │       skills: ["AWS", "Azure"]
                              │     }
                              │   ]
                              │ }
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│                      Frontend Processing                          │
│  1. Transform array to object:                                    │
│     {                                                             │
│       "Programming": ["Java", "Python"],                          │
│       "Cloud": ["AWS", "Azure"]                                   │
│     }                                                             │
│  2. Update UI → setGeneratedSkills(transformed)                  │
│  3. Update context → setGeneratorOutput(...)                     │
└──────────────────────────────────────────────────────────────────┘
```

## State Management Flow

```
┌────────────────────────────────────────────────────────────────┐
│                       ResumeContext                             │
│                    (Context Provider)                           │
└────────────────────────────────────────────────────────────────┘
                             │
                             │ Provides
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  Experience  │    │   Project    │    │    Skills    │
│  Generator   │    │   Generator  │    │   Generator  │
└──────────────┘    └──────────────┘    └──────────────┘
        │                    │                    │
        │ Writes             │ Writes             │ Reads
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────────────────────────────────────────────┐
│              Context State Updates                    │
│                                                       │
│  • setRawExperienceData(data) ──────────┐           │
│  • setRawProjectData(data) ──────────┐   │          │
│  • setGeneratorOutput(output)         │   │          │
│                                       │   │          │
│  ┌──────────────────────────────────┐│   │          │
│  │ rawExperienceData ◄───────────────┘   │          │
│  │ rawProjectData ◄──────────────────────┘          │
│  │ generatorOutput: {                               │
│  │   experiences: [...],                            │
│  │   projects: [...],                               │
│  │   skills: {...}                                  │
│  │ }                                                │
│  └──────────────────────────────────────────────────┘
└──────────────────────────────────────────────────────┘
```

## API Request/Response Transformation

### Experience Generator

```
User Input                    API Request                   API Response
──────────                    ───────────                   ────────────
Job Role: "SDE2"         ───▶ job_role                 ◀─── experience_id: 1
Job Desc: "..."          ───▶ job_description          ◀─── experience_role: "..."
Bullet Counts: [5,4]     ───▶ points_count: [5,4]     ◀─── experience_company_name: "..."
Side Prompt: "..."       ───▶ additional_instruction   ◀─── resume_points: [...]
                                                       ◀─── start_date: "..."
                                                       ◀─── end_date: "..."

                              Frontend Transform
                              ──────────────────
                              companyName ← experience_company_name
                              newExperience ← resume_points
```

### Project Generator

```
User Input                    API Request                   API Response
──────────                    ───────────                   ────────────
Job Role: "SDE2"         ───▶ job_role                 ◀─── project_id: 1
Job Desc: "..."          ───▶ job_description          ◀─── project_name: "..."
Line Counts: [3,2]       ───▶ points_count: [3,2]     ◀─── project_points: [...]
Side Prompt: "..."       ───▶ additional_instruction   ◀─── project_skills: [...]

                              Frontend Transform
                              ──────────────────
                              projectName ← project_name
                              newProjectInfo ← project_points.join(' ')
```

### Skills Generator

```
User Input                      API Request                     API Response
──────────                      ───────────                     ────────────
Job Role: "SDE2"           ───▶ job_role                   ◀─── [
Job Desc: "..."            ───▶ job_description            ◀───   {
Web Research: ☑            ───▶ include_web_research      ◀───     skill_category: "...",
Side Prompt: "..."         ───▶ additional_instruction    ◀───     skills: [...]
                                                           ◀───   }
Context: rawExperienceData ───▶ experience_data           ◀─── ]
Context: rawProjectData    ───▶ project_data

                                Frontend Transform
                                ──────────────────
                                Array → Object
                                {
                                  "Programming": [...],
                                  "Cloud": [...]
                                }
```

## Sequential Generation Flow

```
┌─────────────┐
│   Step 1    │  Experience Generator
│             │  ────────────────────────────────────────┐
│ ✨ Generate │                                           │
└─────────────┘                                           │
      │                                                   │
      │ Generates experiences                             │
      │ Stores rawExperienceData in context              │
      │                                                   │
      ▼                                                   │
┌─────────────┐                                           │
│   Step 2    │  Project Generator                        │
│             │  ────────────────────────────────────┐    │
│ ✨ Generate │                                       │    │
└─────────────┘                                       │    │
      │                                               │    │
      │ Generates projects                            │    │
      │ Stores rawProjectData in context             │    │
      │                                               │    │
      ▼                                               │    │
┌─────────────┐                                       │    │
│   Step 3    │  Skills Generator                     │    │
│             │  Uses BOTH                            │    │
│ ✨ Generate │  • rawExperienceData ◄────────────────┘    │
│             │  • rawProjectData ◄────────────────────────┘
└─────────────┘
      │
      │ Generates categorized skills
      │ Uses previous data for context
      │
      ▼
┌─────────────┐
│   Step 4    │  Resume Output
│             │
│ 📄 Export   │  Combines all generated content
│             │  Edit → Export Word/PDF
└─────────────┘
```

## Error Handling Flow

```
┌──────────────┐
│ User Action  │ Click "Generate"
└──────────────┘
       │
       ▼
┌──────────────────────────────┐
│ setIsGenerating(true)        │
│ setError(null)               │
└──────────────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ try {                        │
│   const response = await     │
│   api.generate(data)         │
│ }                            │
└──────────────────────────────┘
       │
       ├─── Success ──────────────┐
       │                          │
       │                          ▼
       │                   ┌──────────────────┐
       │                   │ Process response │
       │                   │ Update UI        │
       │                   │ setHasGenerated  │
       │                   └──────────────────┘
       │
       └─── Error ────────────────┐
                                  │
                                  ▼
                           ┌──────────────────┐
                           │ catch (err) {    │
                           │   setError(...)  │
                           │ }                │
                           └──────────────────┘
                                  │
       ┌──────────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ finally {                    │
│   setIsGenerating(false)     │
│ }                            │
└──────────────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ UI Updates:                  │
│ • Hide loading spinner       │
│ • Show error message OR      │
│ • Show generated content     │
└──────────────────────────────┘
```

## Files Organization

```
Frontend/
├── src/
│   ├── services/
│   │   └── api.ts ───────────────────── ✨ API service functions
│   │
│   ├── context/
│   │   └── ResumeContext.tsx ─────────── ✨ Raw data storage
│   │
│   └── pages/
│       └── ResumeGenerator/
│           ├── ExperienceGenerator.tsx ── ✨ Step 1 integration
│           ├── ProjectGenerator.tsx ───── ✨ Step 2 integration
│           ├── SkillsGenerator.tsx ────── ✨ Step 3 integration
│           └── ResumeOutput.tsx ────────── Step 4 (unchanged)
│
├── doc/
│   ├── GENERATOR_API_INTEGRATION.md ──── 📖 Complete documentation
│   ├── INTEGRATION_DIAGRAM.md ─────────── 📖 This file
│   └── RESUME_GENERATOR.md ────────────── 📖 Feature docs
│
├── INTEGRATION_SUMMARY.md ───────────────── 📖 Quick summary
└── API_QUICK_REFERENCE.md ───────────────── 📖 Quick reference

✨ = Modified for API integration
📖 = Documentation
```

---

**Legend:**
- `──▶` : Data flow direction
- `◀──` : Data return direction
- `│` : Connection/relationship
- `✨` : Modified file
- `📖` : Documentation file

**Last Updated:** November 19, 2024  
**Version:** 1.0

